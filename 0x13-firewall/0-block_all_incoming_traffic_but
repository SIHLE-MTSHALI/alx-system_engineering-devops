#!/usr/bin/env bash

# Block all incoming traffic
ufw default deny incoming

# Allow incoming traffic on ports 22, 443, and 80
ufw allow 22/tcp
ufw allow 443/tcp
ufw allow 80/tcp

# Enable the firewall
ufw enable
