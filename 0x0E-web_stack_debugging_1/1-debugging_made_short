#!/usr/bin/env bash
# Fix Nginx installation to listen on port 80 (short version)

apt-get update
apt-get install -y nginx
service nginx start
echo "server {
    listen 80;
    server_name localhost;

    location / {
        return 200 'Welcome to Nginx!';
    }
}" > /etc/nginx/sites-available/default
service nginx restart
